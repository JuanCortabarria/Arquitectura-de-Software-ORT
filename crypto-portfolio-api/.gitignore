# Dependencias — se regeneran con "npm install"
node_modules/

# Código compilado — se regenera con "npm run build"
dist/

# Variables de entorno — contiene datos sensibles, NUNCA subir a GitHub
# Usar .env.example como referencia
.env

# Archivos del sistema operativo macOS
.DS_Store

# Logs de npm
*.log
npm-debug.log*
